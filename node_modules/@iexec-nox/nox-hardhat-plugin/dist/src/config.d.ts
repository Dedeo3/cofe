import type { HardhatUserConfig } from "hardhat/config";
import type { HardhatConfig } from "hardhat/types/config";
import type { HardhatUserConfigValidationError } from "hardhat/types/hooks";
export declare const NOX_HOST_NETWORK = "noxHost";
export declare const NOX_LOCAL_NETWORK = "noxLocal";
export declare const NOX_LOCAL_PORT = 8545;
export declare function validatePluginConfig(userConfig: HardhatUserConfig): Promise<HardhatUserConfigValidationError[]>;
/**
 * Returns a copy of `userConfig` with the plugin's internal networks injected:
 *   - `noxHost`: EDR-simulated, backs the JSON-RPC server we spawn.
 *   - `noxLocal`: HTTP, points at the local server (chainId 31337).
 * User-defined entries with the same names win (last spread).
 */
export declare function withInjectedNetworks(userConfig: HardhatUserConfig): HardhatUserConfig;
export declare function resolvePluginConfig(userConfig: HardhatUserConfig, partiallyResolvedConfig: HardhatConfig): Promise<HardhatConfig>;
//# sourceMappingURL=config.d.ts.map