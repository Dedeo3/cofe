import { readFile } from "node:fs/promises";
/**
 * Loads a Hardhat-compiled artifact (ABI + creation/deployed bytecode) from an
 * absolute path.
 */
export async function loadDeploymentArtifact(filePath) {
    const raw = await readFile(filePath, "utf-8").catch((err) => {
        throw new Error(`[nox] Could not read artifact at ${filePath}. Underlying error: ${String(err)}`);
    });
    return JSON.parse(raw);
}
//# sourceMappingURL=artifacts.js.map