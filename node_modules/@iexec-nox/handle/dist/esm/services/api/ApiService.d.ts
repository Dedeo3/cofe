import type { EIP712TypedData } from '../../services/blockchain/IBlockchainService.js';
import type { EthereumAddress, BaseUrl, HexString } from '../../types/internalTypes.js';
import type { IApiService, QueryParameters, Headers, Body, ResponseData, ExpectedResponse } from './IApiService.js';
export declare class ApiService implements IApiService {
    private readonly baseUrl;
    private readonly attestationConfig;
    constructor(baseUrl: BaseUrl, attestationConfig: {
        gatewayAddress: EthereumAddress;
        chainId: number;
        verifyTypedData: (data: EIP712TypedData, signature: HexString) => Promise<EthereumAddress>;
    });
    get({ endpoint, query, headers, timeout, expectedResponse, }: {
        endpoint: string;
        query?: QueryParameters;
        headers?: Headers;
        timeout?: number;
        expectedResponse: ExpectedResponse;
    }): Promise<ResponseData>;
    post({ endpoint, query, body, headers, timeout, expectedResponse, }: {
        endpoint: string;
        query?: QueryParameters;
        body?: Body;
        headers?: Headers;
        timeout?: number;
        expectedResponse: ExpectedResponse;
    }): Promise<ResponseData>;
}
//# sourceMappingURL=ApiService.d.ts.map