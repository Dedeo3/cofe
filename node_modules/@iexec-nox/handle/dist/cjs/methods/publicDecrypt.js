"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.publicDecrypt = publicDecrypt;
const isPubliclyDecryptable_abi_js_1 = require("../services/blockchain/abis/isPubliclyDecryptable.abi.js");
const encoding_js_1 = require("../utils/encoding.js");
const error_js_1 = require("../utils/error.js");
const hex_js_1 = require("../utils/hex.js");
const retry_js_1 = require("../utils/retry.js");
const types_js_1 = require("../utils/types.js");
const validators_js_1 = require("../utils/validators.js");
const viewACL_js_1 = require("./viewACL.js");
async function publicDecrypt({ handle, apiService, blockchainService, subgraphService, config, }) {
    (0, validators_js_1.assertRequiredParams)({ handle }, ['handle']);
    const chainId = await blockchainService.getChainId();
    const chainIdFromHandle = (0, types_js_1.handleToChainId)(handle);
    if (chainIdFromHandle !== chainId) {
        throw new Error(`Handle chainId (${chainIdFromHandle}) does not match connected chainId (${chainId})`);
    }
    const isPubliclyDecryptable = await blockchainService.readContract(config.smartContractAddress, isPubliclyDecryptable_abi_js_1.IS_PUBLICLY_DECRYPTABLE_ABI, [handle]);
    if (!isPubliclyDecryptable) {
        throw new Error(`Handle (${handle}) does not exist or is not publicly decryptable`);
    }
    const solidityType = (0, types_js_1.handleToSolidityType)(handle);
    let handleVerifiedToExist = false;
    const getDecryptionResult = async () => {
        const response = await apiService.get({
            endpoint: `/v0/public/${handle}`,
            expectedResponse: {
                types: {
                    PublicDecryptionResult: [{ name: 'decryptionProof', type: 'string' }],
                },
                primaryType: 'PublicDecryptionResult',
            },
        });
        if (response.status === 404) {
            if (!handleVerifiedToExist && !(0, types_js_1.isUniqueHandle)(handle)) {
                await (0, viewACL_js_1.viewACL)({
                    subgraphService,
                    blockchainService,
                    handle,
                }).catch((error) => {
                    if (error instanceof error_js_1.UnknownHandleError) {
                        throw error;
                    }
                    throw new Error('Failed to decrypt, handle existence is not verified.', {
                        cause: error,
                    });
                });
            }
            handleVerifiedToExist = true;
            throw new error_js_1.NotYetComputedHandleError(handle);
        }
        return response;
    };
    const response = await (0, retry_js_1.retry)(getDecryptionResult, {
        delay: 1000,
        backoff: 2,
        maxRetries: 3,
        shouldRetry: (error) => error instanceof error_js_1.NotYetComputedHandleError,
    });
    const { decryptionProof } = validateApiResponse({
        status: response.status,
        data: response.data,
    });
    const plaintext = `0x${decryptionProof.slice(2 + 65 * 2)}`;
    let value;
    try {
        value = (0, encoding_js_1.decodeValue)(plaintext, solidityType);
    }
    catch (error) {
        throw new Error(`Failed to decode decrypted plaintext: expected hex encoded ${solidityType}, got ${plaintext}`, { cause: error });
    }
    return { value, solidityType, decryptionProof };
}
function validateApiResponse({ status, data, }) {
    if (status !== 200 ||
        typeof data !== 'object' ||
        data === null ||
        !(0, hex_js_1.isHexString)(data?.decryptionProof) ||
        data.decryptionProof.length < 2 + 65 * 2) {
        throw new Error(`Unexpected response from Handle Gateway (status: ${status}, data: ${JSON.stringify(data)})`);
    }
    const { decryptionProof } = data;
    return { decryptionProof };
}
//# sourceMappingURL=publicDecrypt.js.map