"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.viewACL = viewACL;
const viewACL_js_1 = require("../services/subgraph/queries/viewACL.js");
const error_js_1 = require("../utils/error.js");
const retry_js_1 = require("../utils/retry.js");
const validators_js_1 = require("../utils/validators.js");
async function viewACL({ subgraphService, blockchainService, handle, }) {
    (0, validators_js_1.assertRequiredParams)({ handle }, ['handle']);
    const currentBlockNumber = await blockchainService.getBlockNumber();
    const getACLFromSubgraph = async () => {
        const response = (await subgraphService.request(viewACL_js_1.VIEW_ACL_QUERY, {
            handleId: handle.toString(),
        }));
        if (!response ||
            typeof response !== 'object' ||
            !('_meta' in response) ||
            !response._meta ||
            !('block' in response._meta) ||
            typeof response._meta.block !== 'object' ||
            !response._meta.block ||
            !('number' in response._meta.block) ||
            typeof response._meta.block.number !== 'number' ||
            !('handle' in response)) {
            throw new Error('Invalid response from subgraph');
        }
        if (response._meta.block.number < currentBlockNumber) {
            throw new error_js_1.SubgraphOutOfSyncError({
                currentBlock: currentBlockNumber,
                subgraphBlock: response._meta.block.number,
            });
        }
        return response;
    };
    const response = await (0, retry_js_1.retry)(getACLFromSubgraph, {
        delay: 1000,
        backoff: 2,
        maxRetries: 3,
        shouldRetry: (error) => error instanceof error_js_1.SubgraphOutOfSyncError && error.lag < 10,
    });
    if (response.handle === null) {
        throw new error_js_1.UnknownHandleError(handle);
    }
    const admins = response.handle.admins?.map((admin) => admin.account) ?? [];
    const viewers = response.handle.viewers?.map((viewer) => viewer.account) ?? [];
    return {
        isPublic: response.handle.isPubliclyDecryptable ?? false,
        admins,
        viewers,
    };
}
//# sourceMappingURL=viewACL.js.map